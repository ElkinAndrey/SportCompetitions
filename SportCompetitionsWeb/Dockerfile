# При помощи FROM указываем образ, который хотим 
# использовать, чтобы запустилось приложение в 
# контейнере (18.12.1 - версия node js, alpine - дистрибутив linux, который служит для создания docker-образа)
FROM node:18.12.1-alpine
# WORKDIR создает и задает рабочую папку в контейнере
WORKDIR /app

COPY package.json /app

RUN npm install

# Копирует текущее содержимое директории в рабочую папку (/app)
COPY . .
# Сборка проекра
RUN npm run build
# Запуск проекта в контейнере
CMD ["npm", "start"]